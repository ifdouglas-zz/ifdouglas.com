import styled from "styled-components"
import Slick from "react-slick"

const Slider = styled(Slick)`
  width: 330px;
  margin-left: 15px;

  svg {
    margin-bottom: 50px;
  }
`

export default {
  Slider,
}
